<Page
    x:Class="AIM.Views.ScansPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:AIM.Models"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:converters="using:AIM.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToSymbolConverter x:Key="BoolToSymbolConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <Style x:Key="DangerMenuItemStyle" TargetType="MenuFlyoutItem" BasedOn="{StaticResource DefaultMenuFlyoutItemStyle}">
            <Setter Property="Foreground" Value="{ThemeResource SystemFillColorCriticalBrush}"/>
        </Style>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBlock Text="Scans" Style="{ThemeResource TitleTextBlockStyle}"/>
            <Button Command="{x:Bind ViewModel.GoUpCommand}" IsEnabled="{x:Bind ViewModel.IsGoUpEnabled, Mode=OneWay}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE74A;" FontSize="16"/>
                    <TextBlock Text="Up"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.RefreshCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                    <TextBlock Text="Refresh"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.CreateNewFolderCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE8F4;" FontSize="16"/>
                    <TextBlock Text="New Folder"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <ItemsRepeater Grid.Row="1" ItemsSource="{x:Bind ViewModel.Breadcrumbs}" Margin="0,0,0,8">
            <ItemsRepeater.Layout>
                <StackLayout Orientation="Horizontal"/>
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="models:BreadcrumbItem">
                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                        <Button DataContext="{x:Bind}" Click="BreadcrumbButton_Click" Style="{ThemeResource TextBlockButtonStyle}" Visibility="{x:Bind (x:Boolean)IsLast, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold"/>
                        </Button>
                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" Visibility="{x:Bind IsLast, Mode=OneWay}" VerticalAlignment="Center" Padding="8,4"/>
                        <TextBlock Text="/" VerticalAlignment="Center" FontWeight="SemiBold" Visibility="{x:Bind (x:Boolean)IsLast, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>

        <TextBox Grid.Row="2" Text="{x:Bind ViewModel.FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Type to filter files..." Margin="0,0,0,8"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Grid.ColumnSpan="3" Padding="12,8" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Command="{x:Bind ViewModel.SortCommand}" CommandParameter="Name" Style="{ThemeResource TextBlockButtonStyle}" HorizontalAlignment="Left">
                        <TextBlock Text="Name" FontWeight="SemiBold"/>
                    </Button>
                    <Button Grid.Column="1" Command="{x:Bind ViewModel.SortCommand}" CommandParameter="Date" Style="{ThemeResource TextBlockButtonStyle}" HorizontalAlignment="Left">
                        <TextBlock Text="Date Modified" FontWeight="SemiBold"/>
                    </Button>
                    <Button Grid.Column="2" Command="{x:Bind ViewModel.SortCommand}" CommandParameter="Size" Style="{ThemeResource TextBlockButtonStyle}" HorizontalAlignment="Right">
                        <TextBlock Text="Size" FontWeight="SemiBold"/>
                    </Button>
                </Grid>
            </Grid>

            <Grid Grid.Row="1" Grid.ColumnSpan="3">
                <ListView
                    x:Name="ItemsListView"
                    ItemsSource="{x:Bind ViewModel.CurrentItems}"
                    SelectionMode="Multiple"
                    HorizontalContentAlignment="Stretch"
                    DoubleTapped="ItemsListView_DoubleTapped"
                    IsItemClickEnabled="True"
                    IsRightTapEnabled="True"  
                    ItemClick="ItemsListView_ItemClick"
                    Visibility="{x:Bind ViewModel.IsListVisible, Mode=OneWay}">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <interactivity:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Loaded">
                            <core:InvokeCommandAction Command="{x:Bind ViewModel.PageLoadedCommand}"/>
                        </core:EventTriggerBehavior>
                        <core:EventTriggerBehavior EventName="SelectionChanged">
                            <core:InvokeCommandAction Command="{x:Bind ViewModel.SelectionChangedCommand}" CommandParameter="{x:Bind ItemsListView.SelectedItems, Mode=OneWay}"/>
                        </core:EventTriggerBehavior>
                    </interactivity:Interaction.Behaviors>

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ScanTreeItem">
                            <Grid Padding="12,8" HorizontalAlignment="Stretch" RightTapped="ItemGrid_RightTapped">
                                <FlyoutBase.AttachedFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Text="Open" Click="MenuOpen_Click" Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE8B7;"/>
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem Text="Preview" Click="MenuPreview_Click" Visibility="{Binding IsFolder, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE71E;"/>
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem Text="Copy Path" Click="MenuCopyPath_Click">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE8C8;"/>
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem Text="Rename" Click="MenuRename_Click">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE8AC;"/>
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutSeparator/>
                                        <MenuFlyoutItem Text="Delete" Click="MenuDelete_Click" Style="{ThemeResource DangerMenuItemStyle}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE74D;"/>
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </FlyoutBase.AttachedFlyout>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="100" />
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0">
                                    <!-- FIX: Replaced broken Opacity binding with a correct Visibility binding -->
                                    <StackPanel Orientation="Horizontal" Spacing="4" Visibility="{x:Bind IsRenaming, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}">
                                        <FontIcon Glyph="{x:Bind IsFolder, Converter={StaticResource BoolToSymbolConverter}}" FontSize="16"/>
                                        <TextBlock Text="{x:Bind Name}" ToolTipService.ToolTip="{x:Bind FullPath}" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <TextBox
                                        Text="{x:Bind Name, Mode=OneWay}"
                                        Visibility="{x:Bind IsRenaming, Mode=OneWay}"
                                        KeyDown="RenameTextBox_KeyDown"
                                        LostFocus="RenameTextBox_LostFocus"
                                        Loaded="RenameTextBox_Loaded"
                                        Padding="2,0"
                                        BorderThickness="1"/>
                                </Grid>

                                <TextBlock Grid.Column="1" Text="{x:Bind ModifiedDateString}"/>
                                <TextBlock Grid.Column="2" Text="{x:Bind SizeString}" HorizontalAlignment="Right"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="60" Height="60"/>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12" Visibility="{x:Bind ViewModel.IsEmptyMessageVisible, Mode=OneWay}">
                    <FontIcon Glyph="&#xE7C3;" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                    <TextBlock Text="This folder is empty." Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>