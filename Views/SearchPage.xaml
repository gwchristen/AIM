<Page
    x:Class="AIM.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <TextBox Text="{x:Bind ViewModel.SearchDirectory, Mode=TwoWay}" PlaceholderText="Search Directory" Width="400" Margin="0,0,5,0"/>
            <Button Content="Browse" Command="{x:Bind ViewModel.BrowseCommand}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <TextBox x:Name="SearchTextBox" Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay}" Width="300" PlaceholderText="Enter search query" Margin="0,0,5,0"/>
            <Button Content="Search" Command="{x:Bind ViewModel.SearchCommand}"/>
        </StackPanel>
        <ToggleSwitch Grid.Row="2" Header="Search in file contents" IsOn="{x:Bind ViewModel.IsContentSearch, Mode=TwoWay}" Margin="5"/>
        <Border Grid.Row="3" Background="Transparent" Margin="5">
            <TextBlock Text="{Binding StatusMessage}" Foreground="Red" FontWeight="Bold" />
        </Border>
        <ListView Grid.Row="4" ItemsSource="{x:Bind ViewModel.SearchResults}" SelectionChanged="ResultsListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Width="300" />
                        <TextBlock Text="{Binding FullPath}" Width="400" />
                        <TextBlock Text="{Binding Type}" Width="100" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>