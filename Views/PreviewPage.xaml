<Page
    x:Class="AIM.Views.PreviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="{Binding FileName}" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" Text="{Binding Content, Mode=TwoWay}" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,8"/>
        <Button Grid.Row="2" Command="{x:Bind ViewModel.SaveFileContentCommand}" HorizontalAlignment="Right" Style="{ThemeResource AccentButtonStyle}">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE74E;" FontSize="16"/>
                <TextBlock Text="Save"/>
            </StackPanel>
        </Button>
    </Grid>
</Page>