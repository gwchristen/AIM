<UserControl
    x:Class="AIM.Views.AdminTools.BatchRenamerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <!-- The DataContext is now set in the code-behind -->

    <StackPanel Spacing="12">
        <TextBlock Text="Batch Renamer" Style="{StaticResource TitleTextBlockStyle}"/>
        <TextBlock Text="Sequentially renames all files within each OpCo subfolder of a selected parent directory. This is a permanent action." TextWrapping="Wrap"/>

        <TextBlock Text="Select Parent Directory" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,10,0,0"/>
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBox Text="{Binding RenameDirectory, Mode=OneWay}" IsReadOnly="True" PlaceholderText="Select the parent directory..." MinWidth="300"/>
            <Button Content="Browse..." Command="{Binding SelectRenameDirectoryCommand}"/>
        </StackPanel>

        <Button Content="Begin Batch Rename" Command="{Binding RenameFilesCommand}" HorizontalAlignment="Left" Style="{StaticResource AccentButtonStyle}" Margin="0,20,0,0"/>
    </StackPanel>
</UserControl>