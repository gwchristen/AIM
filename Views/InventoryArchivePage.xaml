<Page
    x:Class="AIM.Views.InventoryArchivePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:AIM.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodels:InventoryArchiveViewModel}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Inventory Archive" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,24"/>

        <StackPanel Grid.Row="1" Spacing="12">
            <TextBlock Text="1. Select a folder to archive" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="Select Folder..." Command="{Binding SelectFolderCommand}"/>
                <TextBlock Text="{Binding SelectedFolderToArchive, Mode=OneWay, FallbackValue='No folder selected'}" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Content="2. Archive Selected Folder" 
                    Command="{Binding ArchiveFolderCommand}"
                    IsEnabled="{Binding IsFolderSelected, Mode=OneWay}"
                    HorizontalAlignment="Left"
                    Style="{ThemeResource AccentButtonStyle}"
                    Margin="0,12,0,0"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,24,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Archived Inventories" Style="{ThemeResource SubtitleTextBlockStyle}"/>
            <ListView Grid.Row="1" 
                      Margin="0,12,0,0"
                      ItemsSource="{Binding ArchivedDirectories, Mode=OneWay}"
                      IsItemClickEnabled="True"
                      DoubleTapped="ArchivedListView_DoubleTapped"
                      SelectionMode="Single"
                      CanDragItems="False">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Padding="0,8"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>