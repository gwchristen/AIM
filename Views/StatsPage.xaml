<Page
    x:Class="AIM.Views.StatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Statistics" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,16"/>

        <!-- Stats Display -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Header row -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Total Inventory Count" Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="OpCo Inventory Stats" Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,12"/>

            <!-- Row 2: Files -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding TotalTextFilesText}" HorizontalAlignment="Center" Margin="0,4"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding OhioFilesText}" HorizontalAlignment="Center" Margin="0,4"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding ImFilesText}" HorizontalAlignment="Center" Margin="0,4"/>

            <!-- Row 3: Devices -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding TotalLinesText}" HorizontalAlignment="Center" Margin="0,4"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding OhioLinesText}" HorizontalAlignment="Center" Margin="0,4"/>
            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding ImLinesText}" HorizontalAlignment="Center" Margin="0,4"/>
        </Grid>

        <!-- Problematic Files -->
        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock Text="Files with Lines !=17 Characters (Double-click to Preview)" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
            <ListView ItemsSource="{Binding ProblematicFiles}" DoubleTapped="ListBox_DoubleTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>