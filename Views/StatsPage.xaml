<Page
    x:Class="AIM.Views.StatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Stats Display -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Header row -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Total Inventory Count" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="OpCo Inventory Stats" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/>

            <!-- Row 2: Files -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding TotalTextFilesText}" HorizontalAlignment="Center"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding OhioFilesText}" HorizontalAlignment="Center"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding ImFilesText}" HorizontalAlignment="Center"/>

            <!-- Row 3: Devices -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding TotalLinesText}" HorizontalAlignment="Center"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding OhioLinesText}" HorizontalAlignment="Center"/>
            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding ImLinesText}" HorizontalAlignment="Center"/>
        </Grid>

        <!-- Problematic Files -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Files with Lines !=17 Characters (Double-click to Preview)" FontSize="16"/>
            <ListBox ItemsSource="{Binding ProblematicFiles}" DoubleTapped="ListBox_DoubleTapped">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Page>