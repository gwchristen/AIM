<Page
    x:Class="AIM.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="24" Spacing="16">
            <TextBlock Text="Settings" Style="{ThemeResource TitleTextBlockStyle}"/>
            
            <StackPanel Spacing="8">
                <TextBlock Text="Security" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBox Header="Password" Text="{x:Bind ViewModel.Password, Mode=TwoWay}" Width="200"/>
                    <Button Content="Lock" Click="LockButton_Click" VerticalAlignment="Bottom"/>
                    <Button Content="Unlock" Click="UnlockButton_Click" VerticalAlignment="Bottom"/>
                    <Button Click="SaveButton_Click" Style="{ThemeResource AccentButtonStyle}" VerticalAlignment="Bottom">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE74E;" FontSize="16"/>
                            <TextBlock Text="Save"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
            
            <StackPanel Spacing="12">
                <TextBlock Text="Directories" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <StackPanel Spacing="8">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox Header="Root Directory" Text="{x:Bind ViewModel.DefaultRootDirectory, Mode=TwoWay}" Width="400" IsEnabled="{x:Bind IsLocked, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>
                        <Button Click="BrowseRootDirectory_Click" VerticalAlignment="Bottom">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8DA;" FontSize="16"/>
                                <TextBlock Text="Browse"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox Header="Archive Path" Text="{x:Bind ViewModel.ArchivePath, Mode=TwoWay}" Width="400" IsEnabled="{x:Bind IsLocked, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>
                        <Button Click="BrowseArchivePath_Click" VerticalAlignment="Bottom">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8DA;" FontSize="16"/>
                                <TextBlock Text="Browse"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox Header="Shipped Directory" Text="{x:Bind ViewModel.ShippedDirectory, Mode=TwoWay}" Width="400" IsEnabled="{x:Bind IsLocked, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>
                        <Button Click="BrowseShippedDirectory_Click" VerticalAlignment="Bottom">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8DA;" FontSize="16"/>
                                <TextBlock Text="Browse"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox Header="File Scans Directory" Text="{x:Bind ViewModel.FileScansDirectory, Mode=TwoWay}" Width="400"/>
                        <Button Click="BrowseFileScansDirectory_Click" VerticalAlignment="Bottom">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8DA;" FontSize="16"/>
                                <TextBlock Text="Browse"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox Header="Inventory Archive Directory" Text="{x:Bind ViewModel.InventoryArchiveDirectory, Mode=TwoWay}" Width="400" IsEnabled="{x:Bind IsLocked, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>
                        <Button Click="BrowseInventoryArchiveDirectory_Click" VerticalAlignment="Bottom">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8DA;" FontSize="16"/>
                                <TextBlock Text="Browse"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>