<Page
    x:Class="AIM.Views.InventoryViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:AIM.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodels:InventoryViewerViewModel}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Command="{Binding GoBackCommand}" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE72B;" FontSize="14"/>
                <TextBlock Text="Back to Archive List"/>
            </StackPanel>
        </Button>

        <TextBlock Grid.Row="1" Text="Inventory Viewer" Style="{ThemeResource TitleTextBlockStyle}"/>

        <!-- THE FIX: Add a search box for filtering the TreeView -->
        <AutoSuggestBox Grid.Row="2"
                        x:Name="FilterBox"
                        PlaceholderText="Filter files and folders..."
                        QueryIcon="Find"
                        Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,12,0,0"/>

        <TreeView Grid.Row="3"
                  ItemsSource="{Binding RootNodes, Mode=OneWay}"
                  Margin="0,12,0,0">
            <TreeView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:TreeViewNode">
                    <!-- THE FIX: Bind Visibility to allow the ViewModel to show/hide items -->
                    <TreeViewItem ItemsSource="{Binding Children}" 
                                  Content="{Binding Content}" 
                                  Visibility="{Binding Visibility, Mode=OneWay}"/>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Page>