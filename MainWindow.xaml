<Window
    x:Class="AIM.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AIM">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical" Margin="10,10,10,10">
            <RadioButton Content="Select Root" GroupName="Navigation" Style="{StaticResource TileRadioButton}" Click="SelectCustomRootButton_Click"/>
            <RadioButton Content="Refresh Tree" GroupName="Navigation" Style="{StaticResource TileRadioButton}" Command="{x:Bind ViewModel.RefreshTreeCommand}"/>
            <RadioButton Content="Browse" GroupName="Navigation" Style="{StaticResource TileRadioButton}" IsChecked="True" Click="BrowseButton_Click"/>
            <RadioButton Content="Preview" GroupName="Navigation" Style="{StaticResource TileRadioButton}" Click="PreviewButton_Click"/>
            <RadioButton Content="Search" GroupName="Navigation" Style="{StaticResource TileRadioButton}" Click="SearchButton_Click"/>
            <RadioButton Content="Scans" GroupName="Navigation" Style="{StaticResource TileRadioButton}" Click="ScansButton_Click"/>
            <RadioButton Content="Inv Archives" GroupName="Navigation" Style="{StaticResource TileRadioButton}" Click="InvArchivesButton_Click"/>
            <RadioButton Content="Stats" GroupName="Navigation" Style="{StaticResource TileRadioButton}" Click="StatsButton_Click"/>
            <RadioButton Content="Settings" GroupName="Navigation" Style="{StaticResource TileRadioButton}" Click="SettingsButton_Click"/>
        </StackPanel>

        <Frame x:Name="MainFrame" x:FieldModifier="public" Grid.Column="1" />
    </Grid>
</Window>