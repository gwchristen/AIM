<Window
    x:Class="AIM.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10">
                <Button Content="Select Root Directory" Click="SelectCustomRootButton_Click" Margin="0,0,0,10"/>
                <Button Content="Refresh Tree" Click="RefreshTreeButton_Click" Margin="0,0,0,10"/>
                <TreeView x:Name="DirectoryTree" ItemsSource="{x:Bind ViewModel.DirectoryItems}" ItemInvoked="DirectoryTree_ItemInvoked">
                    <TreeView.ItemTemplate>
                        <DataTemplate>
                            <TreeViewItem ItemsSource="{Binding SubDirectories}" IsExpanded="{Binding IsExpanded}">
                                <TextBlock Text="{Binding Name}" />
                            </TreeViewItem>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </StackPanel>
        </ScrollViewer>
        <TabView x:FieldModifier="public" x:Name="FeatureTabs" Grid.Column="1" SelectedIndex="0">
            <TabViewItem Header="Browse">
                <Frame x:FieldModifier="public" x:Name="BrowseFrame"/>
            </TabViewItem>
            <TabViewItem Header="Search">
                <Frame x:FieldModifier="public" x:Name="SearchFrame"/>
            </TabViewItem>
            <TabViewItem Header="Preview">
                <Frame x:FieldModifier="public" x:Name="PreviewFrame"/>
            </TabViewItem>
            <TabViewItem Header="Settings">
                <Frame x:FieldModifier="public" x:Name="SettingsFrame"/>
            </TabViewItem>
        </TabView>
    </Grid>
</Window>