<Page
    x:Class="AIM.Views.ScansPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Directory List -->
        <ListView Grid.Column="0" ItemsSource="{Binding Directories}" SelectedItem="{Binding SelectedDirectory, Mode=TwoWay}" Margin="0,0,16,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8B7;" FontSize="16"/>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Files List with Sort Headers -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Sort Headers -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Command="{Binding SortCommand}" CommandParameter="Name" Padding="8" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="Name" FontWeight="SemiBold"/>
                        <FontIcon Glyph="&#xE74A;" FontSize="12"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="1" Command="{Binding SortCommand}" CommandParameter="Size" Padding="8" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="Size" FontWeight="SemiBold"/>
                        <FontIcon Glyph="&#xE74A;" FontSize="12"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="2" Command="{Binding SortCommand}" CommandParameter="Created" Padding="8" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="Created" FontWeight="SemiBold"/>
                        <FontIcon Glyph="&#xE74A;" FontSize="12"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="3" Command="{Binding SortCommand}" CommandParameter="Modified" Padding="8" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="Modified" FontWeight="SemiBold"/>
                        <FontIcon Glyph="&#xE74A;" FontSize="12"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Files List -->
            <ListView x:ConnectionId='2' Grid.Row="1" x:Name="FilesListView" ItemsSource="{Binding Files}"                                           SelectionMode="Multiple"                                                  >
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" Grid.Column="0" Padding="8,4"/>
                            <TextBlock Text="{Binding SizeString}" Grid.Column="1" Padding="8,4"/>
                            <TextBlock Text="{Binding CreatedDateString}" Grid.Column="2" Padding="8,4"/>
                            <TextBlock Text="{Binding ModifiedDateString}" Grid.Column="3" Padding="8,4"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>

