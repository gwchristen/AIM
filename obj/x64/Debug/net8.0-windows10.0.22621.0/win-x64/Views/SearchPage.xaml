<Page x:ConnectionId='1'
    x:Class="AIM.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Spacing="16">
            <TextBlock Text="Search" Style="{ThemeResource TitleTextBlockStyle}"/>
            
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:ConnectionId='7'                                                        PlaceholderText="Search Directory" Width="400"/>
                <Button x:ConnectionId='8'                                           >
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8DA;" FontSize="16"/>
                        <TextBlock Text="Browse"/>
                    </StackPanel>
                </Button>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:ConnectionId='5' x:Name="SearchTextBox"                                                    Width="400" PlaceholderText="Enter search query"/>
                <Button x:ConnectionId='6'                                            Style="{ThemeResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE721;" FontSize="16"/>
                        <TextBlock Text="Search"/>
                    </StackPanel>
                </Button>
            </StackPanel>
            
            <ToggleSwitch x:ConnectionId='4' Header="Search in file contents"                                                       />
            
            <TextBlock Text="{Binding StatusMessage}" Style="{ThemeResource BodyStrongTextBlockStyle}" Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
        </StackPanel>
        
        <ListView x:ConnectionId='2' Grid.Row="4"                                                                                                    Margin="0,8,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="1" Text="{Binding FullPath}" TextTrimming="CharacterEllipsis" Margin="8,0"/>
                        <TextBlock Grid.Column="2" Text="{Binding Type}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

