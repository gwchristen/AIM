<Page x:ConnectionId='1'
    x:Class="AIM.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:AIM.Models"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="16">
            <TextBlock Text="Search" Style="{ThemeResource TitleTextBlockStyle}"/>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:ConnectionId='12'                                                        PlaceholderText="Search Directory" Width="400"/>
                <Button x:ConnectionId='13'                                           >
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8DA;" FontSize="16"/>
                        <TextBlock Text="Browse"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:ConnectionId='10' x:Name="SearchTextBox"                                                    Width="400" PlaceholderText="Enter search query"/>
                <Button x:ConnectionId='11'                                            Style="{ThemeResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE721;" FontSize="16"/>
                        <TextBlock Text="Search"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <ToggleSwitch x:ConnectionId='9' Header="Search in file contents"                                                       />

            <!-- THIS IS THE FIX: The old TextBlock for status messages has been completely removed. -->

        </StackPanel>

        <ListView x:ConnectionId='2' Grid.Row="2"
                  Margin="0,16,0,0"
                  x:Name="ResultsListView"
                                                                
                  IsItemClickEnabled="True">

            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="DoubleTapped">
                    <core:InvokeCommandAction x:ConnectionId='3' 
                                                                                 
                                                                                             />
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>

            <ListView.ItemTemplate>
                <DataTemplate                             >
                    <Grid x:ConnectionId='5' Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:ConnectionId='6' Grid.Column="0"                      TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:ConnectionId='7' Grid.Column="1"                          TextTrimming="CharacterEllipsis" Margin="8,0"/>
                        <TextBlock x:ConnectionId='8' Grid.Column="2"                     />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

